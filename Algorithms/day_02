
Description:
  Given an Integer array nums, this algorithm finds the subarray with the largest sum, and return its sum.

input: 
    - array of Integers nums
output:
    - sum of the found subarray 
--------------------------------------
n = length(array)
for i in range(1,n)
      if actualSum + array[i] > array[i]
            actualSum = actualSum + array[i]
      else
            actualSum = array[i]
      if actualSum > grandSum
            grandSum = actualSum
  return grandSum
